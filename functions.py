# como funcionan las funciones en python

#primero debemos definir la funcion
def calcular_area(ancho, alto):
    area = ancho * alto
    return area
#luego podemos llamar a la funcion
resultado = calcular_area(5, 10)
print(resultado)